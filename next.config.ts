import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false, // Desactivar para evitar doble mounting en dev
};

export default nextConfig;
